# demo.ps1 / demo.sh 专用的 sidecar 配置：
# 同机运行，静态网关指向本地 mock daemon，绕过 mDNS（多播不回环）。
sidecar:
  mdns_interval: 10s
  dedup_ttl: 3s
  heartbeat_timeout: 60s
  static_gateways:
    - { id: "gw_mock", url: "http://localhost:8082" }
